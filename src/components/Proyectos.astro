---
import NextJS from "./icons/NextJS.astro";
import Tailwind from "./icons/Tailwind.astro";
import JavaScript from "./icons/JavaScript.astro";
import HTML from "./icons/HTML.astro";
import CSS from "./icons/CSS.astro";
import TypeScript from "./icons/TypeScript.astro";

const TAGS = {
    NEXT: {
        name: "Next.js",
        class: "bg-black text-white",
        icon: NextJS,
    },
    TAILWIND: {
        name: "Tailwind CSS",
        class: "bg-[#003159] text-white",
        icon: Tailwind,
    },
    JAVASCRIPT: {
        name: "JavaScript",
        class: "bg-yellow-700 text-black",
        icon: JavaScript,
    },
    TypeScript: {
        name: "TypeScript",
        class: "bg-blue-700 text-white",
        icon: TypeScript,
    },
    HTML: {
        name: "HTML",
        class: "bg-red-700 text-white",
        icon: HTML,
    },
    CSS: {
        name: "CSS",
        class: "bg-blue-700 text-white",
        icon: CSS,
    },
};
const PROJECTS = [
    {
        title: "Caribbean Ind",
        description:
            "Tienda de ropa en línea. Creada con Next.js, React y Tailwind CSS.",
        link: "https://caribbean-ind.vercel.app/",
        github: "https://github.com/Juanmorales1810/caribbean-ind/tree/master",
        image: "/MockupWebCaribbean.webp",
        tags: [TAGS.NEXT, TAGS.TAILWIND, TAGS.TypeScript],
    },
    {
        title: "Rhinestone Project",
        description:
            "Tienda de servicios audiovisuales en línea. Creada con Next.js, React y Tailwind CSS.",
        link: "https://rhinestone-project.vercel.app/",
        image: "/MockupWebRP.webp",
        tags: [TAGS.NEXT, TAGS.TAILWIND, TAGS.TypeScript],
    },
    {
        title: "Ssg Store",
        description:
            "Tienda de juegos en línea. Creada con Next.js, React y Tailwind CSS.",
        link: "https://ssg-new.vercel.app/",
        image: "/MockupWebSsg.webp",
        tags: [TAGS.NEXT, TAGS.TAILWIND, TAGS.TypeScript],
    },
    {
        title: "Cuentas Claras",
        description:
            "Aplicación web para el control de gastos. Creada con HTML, CSS y JavaScript Vanilla.",
        link: "https://juanmorales1810.github.io/CuentasClaras/",
        image: "/MockupWebCC.webp",
        tags: [TAGS.JAVASCRIPT, TAGS.HTML, TAGS.CSS],
    },
];
---

<div class="flex flex-wrap justify-center items-center gap-4 w-full max-w-5xl">
    {
        PROJECTS.map((project) => (
            <article class="relative flex flex-col group justify-between w-full max-w-md h-auto shadow-lg">
                <header class="absolute z-10 p-2">
                    <h2 class="text-xl font-bold text-white">
                        {project.title}
                    </h2>
                </header>
                <section class="overflow-hidden rounded-t-xl">
                    <img
                        src={project.image}
                        alt={project.title}
                        class="w-full h-72 object-cover group-hover:scale-105 transition-all duration-300 ease-in-out z-10"
                    />
                </section>
                <footer class="relative z-10 w-full p-2 bg-violet-900 rounded-b-lg">
                    <div class="flex justify-between items-center flex-wrap mt-2">
                        <ul class="flex flex-row mb-2 gap-x-2">
                            {project.tags.map((tag) => (
                                <li>
                                    <span
                                        class={`flex gap-x-2 rounded-full ${tag.class} text-xs py-1 px-2`}
                                    >
                                        <tag.icon class="size-4" />
                                        {tag.name}
                                    </span>
                                </li>
                            ))}
                        </ul>
                        <a
                            href={project.link}
                            target="_blank"
                            class="px-2 py-1 text-white bg-violet-500 rounded-md hover:bg-violet-600 transition-colors duration-300 ease-in-out font-semibold"
                        >
                            Visitar
                        </a>
                    </div>
                </footer>
                <img
                    src={project.image}
                    alt={project.title}
                    class="absolute w-full h-72 object-cover group-hover:scale-105 transition-all duration-300 ease-in-out -z-10 opacity-0 group-hover:blur-2xl group-hover:opacity-100"
                />
            </article>
        ))
    }
</div>
